# ================================================================
# Core dependencies — compatible with Python 3.8 (Spark base image)
# ================================================================

# Spark for Kafka ingestion
pyspark==3.5.4

# Trino client for analytics queries
trino==0.330.0

# Data manipulation (last version supporting Python 3.8)
pandas==2.0.3
numpy==1.24.4

# Pretty-print ASCII tables
tabulate==0.9.0

# HTTP helpers
requests==2.31.0

# For YAML config (if needed)
pyyaml==6.0.1