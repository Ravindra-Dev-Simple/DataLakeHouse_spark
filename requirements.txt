# ================================================================
# Core dependencies
# ================================================================

# Spark for Kafka ingestion (required only for --mode kafka)
pyspark==3.5.4

# Trino client for analytics queries (required only for --mode trino)
trino==0.330.0

# Data manipulation
pandas==2.2.0

# Pretty-print ASCII tables
tabulate==0.9.0

# For possible HTTP helpers
requests==2.32.3